--// 1. Đợi Game Load hoàn tất
repeat task.wait() until game:IsLoaded() and game.Players.LocalPlayer:FindFirstChild("DataLoaded")

-- Bỏ qua màn hình chọn team (nếu có)
if game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("Main (minimal)") then
    repeat task.wait() until not game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("Main (minimal)")
end

local plr = game.Players.LocalPlayer
local CommF = game.ReplicatedStorage.Remotes.CommF_

--// CẤU HÌNH
local TargetItem = "Dojo Belt (Yellow)" 
local FileName = "YellowBelt-Done" -- Tên file báo về tool
local WaitTime = 60                -- Thời gian chờ trước khi đổi acc (Giây)

--// 2. Hàm kiểm tra item
function checkItemExist(itemName)
    local success, inv = pcall(function()
        return CommF:InvokeServer("getInventory") 
    end)
    
    if success and inv then
        for _, v in pairs(inv) do
            -- So sánh tên item
            if v.Name and (v.Name == itemName or string.find(v.Name, "Dojo Belt (Yellow)")) then 
                return true
            end
        end
    end
    return false
end

--// 3. Vòng lặp kiểm tra
print("🟡 Đang quét Inventory tìm: " .. TargetItem)

task.spawn(function()
    while task.wait(5) do -- Check mỗi 5s
        local hasBelt = checkItemExist(TargetItem)
        
        if hasBelt then
            print("✅ ĐÃ TÌM THẤY ĐAI VÀNG (YELLOW BELT)!")
            
            -- Bước 1: Ghi file báo cáo NGAY LẬP TỨC để đánh dấu
            pcall(function()
                writefile(plr.Name .. ".txt", "Completed-" .. FileName)
                print("💾 Đã ghi file báo cáo: " .. plr.Name .. ".txt")
            end)
            
            -- Bước 2: Đợi 60 giây để Game lưu dữ liệu (Save Data)
            print("⏳ Đang treo thêm " .. WaitTime .. " giây để tránh mất đồ...")
            task.wait(WaitTime)
            
            -- Bước 3: Tắt game để đổi acc
            print("🛑 Đã hết 60s. Shutdown để đổi acc!")
            game:Shutdown() 
            break
        else
            print("⏳ Vẫn chưa có Đai Vàng...")
        end
    end
end)
