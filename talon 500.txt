--// 1. Cấu hình
local TargetName = "Dragon Talon" -- Tên chính xác của món đồ
local TargetMas = 500             -- Số Mastery cần đạt
local FileName = "DragonTalon-500" -- Tên file báo cáo

--// 2. Đợi Game Load
repeat task.wait() until game:IsLoaded()
repeat task.wait() until game.Players.LocalPlayer
local plr = game.Players.LocalPlayer

--// 3. Hàm ghi file báo cáo
function writeFinishFile()
    pcall(function()
        writefile(plr.Name .. ".txt", "Completed-" .. FileName)
    end)
end

--// 4. Hàm kiểm tra Mastery trực tiếp trên người (Cầm tay hoặc trong Balo)
function getEquippedMastery()
    local character = plr.Character or plr.CharacterAdded:Wait()
    local backpack = plr.Backpack
    
    -- Ưu tiên 1: Tìm xem nhân vật có đang cầm trên tay không
    local tool = character:FindFirstChild(TargetName)
    
    -- Ưu tiên 2: Nếu không cầm, tìm trong thanh slot đồ (Backpack)
    if not tool then
        tool = backpack:FindFirstChild(TargetName)
    end

    -- Nếu tìm thấy Tool
    if tool then
        -- Trong Blox Fruits, Tool thường có 1 Value tên là "Level" chứa Mastery
        if tool:FindFirstChild("Level") then
            return true, tool.Level.Value
        else
            -- Trường hợp dự phòng: Một số tool không lưu Level trong đó mà hiển thị trên GUI
            -- Nhưng thường check .Level là chuẩn nhất cho item đang cầm
            return true, 0 -- Tìm thấy nhưng không đọc được Mastery (Lỗi game hoặc sai tên)
        end
    end
    
    return false, 0 -- Không tìm thấy món đồ trên người
end

--// 5. Vòng lặp chính
print("🚀 Đang soi Mastery của: " .. TargetName .. " (Trên tay/Balo)")

while task.wait(2) do -- Check nhanh hơn (2 giây/lần)
    local found, mastery = getEquippedMastery()
    
    if found then
        print("⚔️ Đang cầm/có " .. TargetName .. " | Mastery: " .. mastery)
        
        if mastery >= TargetMas then
            print("✅ Đã ĐỦ 500 Mastery! Ghi file và tắt máy...")
            writeFinishFile()
            task.wait(1)
            game:Shutdown()
            break
        end
    else
        -- Nếu không tìm thấy, thử tự động trang bị (Equip) nếu nó nằm sâu trong Balo
        print("⚠️ Không thấy " .. TargetName .. " trên tay/slot. Đang tìm trong Balo...")
        if plr.Backpack:FindFirstChild(TargetName) then
            plr.Character.Humanoid:EquipTool(plr.Backpack[TargetName])
        end
    end
end